/**
 * Re-export SmartSearchSources and RagSource from SmartSearch module.
 * The component is defined inline in SmartSearch.tsx to keep
 * the streaming component and its source display colocated.
 */
export { SmartSearchSources, type RagSource } from './SmartSearch'
